import java.util.ArrayList;

public class Booth {
    private int age;
    private double height;
    private int price;
    private ArrayList<IConnect> customers;

    public Booth(){
        this.age = age;
        this.height = height;
        this.price = price;
        this.customers = new ArrayList<>();
    }

    public boolean connect(IConnect customers) {
        if(age >= WingCoaster.class(getAge));
    }

}
